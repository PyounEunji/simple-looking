<center>
<body style="background-size:cover; background-color:#ea5537; padding:280px;">

<div class="position">
<h2>로그인</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>


      <div class="field form-group form-group-lg" style="font-family:Arial">
        <%= f.email_field :email, class:"form-control", :placeholder => "이메일", autofocus: true %>
      </div>

      <div class="field form-group form-group-lg" style="font-family:Arial">
        <%= f.password_field :password, class:"form-control", :placeholder => "비밀번호", autocomplete: "off" %>
      </div>
  
  <div class="actions"></div>
    <% if devise_mapping.rememberable? -%>
      <div class="field form-group form-group-lg">
          <%= f.check_box :remember_me%>
          <%= f.label :기억하기 %>
      </div>
  <% end -%>

      <div class="field form-group form-group-lg">
          <%= f.submit "로그인", class:"btn btn-block btn-primary" %>
        </a>
    </div>
     <div class="field form-group form-group-lg">
       <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
      </div>
<% end %>
<%# render "devise/shared/links" %>
</div>
</body>

</center>
<style type="text/css">
.field{ border: none;
                  width: 400px;
                  height: 30px;
                  color: white;
                  font-size: 16px;
                  border-radius: 10px;
                  margin-top: 30px;
                  margin-bottom: 10px;
                }
</style>